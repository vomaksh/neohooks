import { Box, Heading, VStack } from '@chakra-ui/react';

export function WebhookInfo() {
  return (
    <Box flex={1} bgColor="whiteAlpha.30" padding={4} height="full">
      <VStack spacing={2}>
        <Box>
          <Heading>Chintu World</Heading>
        </Box>
      </VStack>
    </Box>
  );
}
